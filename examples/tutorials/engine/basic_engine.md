# Basic Engine Tutorial

The module `atelier-engine` contains the functionality for the Matching Engine, the component in charge of actually producing changes in the orderbook given the order events stream that it will receive. 

```rust
use atelier::data::market::Orderbook;
```

