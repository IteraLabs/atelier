
pub fn main() {

    println!("Matchine Engine");

    // the client generates an order
    // the client needs to have an API implementation to send orders to engine
    // the client forms the event struct
    // the client pushes the event to the engine

    // the engine receives the event
    // the engine queries the state of the orderbook
    // the engine executes the modification of the orderbook
    // the engine registers the action in trades
    
}

